import React from "react";
import GoBackToShop from "./GoBackToShop";
const EmptyCart = () => {
  return (
    <div>
      <span>Your Cart is Empty</span>
    </div>
  );
};

export default EmptyCart;
